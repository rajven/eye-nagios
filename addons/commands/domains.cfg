define  command {
        command_name    check_domain
        command_line	$USER1$/check_domain $ARG1$ $ARG2$
        }

define command {
	command_name	check_dns
	command_line	$USER1$/check_dns -H $ARG1$ -s $HOSTADDRESS$ -a $ARG2$
	}

define command {
	command_name	check_dns_ip
	command_line	$USER1$/check_dns -H $ARG1$ -a $ARG2$
	}
